import { PrismaClient } from '@prisma/client'
import { PrismaPg } from '@prisma/adapter-pg'

const adapter = new PrismaPg({ connectionString: process.env.DATABASE_URL! })
const prisma = new PrismaClient({ adapter })

const articles = [
  {
    title: '给博客加上 AI 评论回复和 AI 助手功能',
    slug: `ai-comment-reply-assistant-${Date.now()}`,
    content: `# 给博客加上 AI 评论回复和 AI 助手功能

最近给自己的博客做了三个 AI 功能：自动回复评论、文章问答、全局 AI 助手。记录一下实现过程和踩过的坑。

## AI 自动回复评论

### 实现原理

用户发表评论后，后端异步调用 Claude API，以博主身份生成回复，存入数据库作为子评论显示。

整个流程：

1. 用户提交评论 → 存入数据库
2. 异步触发 \`createAIReply\` 函数
3. 查文章内容 + 用户评论 → 调用 Claude
4. Claude 生成回复 → 以博主身份存入数据库
5. 前端刷新后显示 AI 回复

提示词让 Claude 扮演博主角色，结合文章内容生成自然友好的回复，控制在 2-3 句话以内。

### 踩过的坑

Vercel Serverless 函数响应返回后进程就结束了，后台异步任务会被直接杀掉。最开始用的是：

\`\`\`typescript
createAIReply(...).catch(() => {})
\`\`\`

不等待，结果 AI 回复一直不出现。解决方法是改成 \`await\`，在响应返回前完成 AI 回复的生成和存储。

### 游客评论支持

顺手把评论系统改造了一下，支持游客不登录直接评论，只需要填写昵称。游客评论同样会触发 AI 自动回复，体验和登录用户一致。

防滥用方面加了 IP 限流，同一 IP 每分钟最多发 3 条评论。

## 文章 AI 问答

文章详情页底部加了一个问答框，读者可以针对这篇文章提问，AI 基于文章内容回答。

实现方式是把文章全文塞进系统提示词，让 Claude 只根据文章内容回答。支持流式输出，打字机效果让等待体验好很多。

对技术文章特别有用，读者看到不懂的地方可以直接问，不用去搜索引擎。

## 全局 AI 助手

右下角有一个悬浮按钮，点击从右侧滑出对话抽屉，支持多轮对话，游客也能用。

### 向量搜索 + 知识图谱

普通 AI 助手不了解博客内容，回答不了"你有哪些文章"这类问题。为此做了两层检索：

**向量搜索**：用 Voyage AI 给每篇文章的小结生成 embedding，存入 Supabase 的 pgvector。用户提问时先找出最相关的 2-3 篇文章，把全文塞进上下文。

**知识图谱**：提取每篇文章的技术概念节点，建立文章之间的关联关系（系列关系、前置知识）。回答时不只推荐相关文章，还能说"建议先看系列一打基础"。

### 模型自动切换

三个功能都调用 Claude API，抽了一个公共函数 \`callClaude\` 统一处理。内置模型优先级列表：

- \`claude-sonnet-4-6\`
- \`claude-sonnet-4-5-20250929\`
- \`gpt-5.3-codex\`

某个模型不可用时自动降级，不会因为单个模型故障影响所有功能。

## 小结

这三个功能让博客从静态内容站变成了有交互感的产品。AI 自动回复让评论区不再冷清，文章问答降低了读者的理解门槛，AI 助手则让博客内容更容易被检索和利用。

整个开发过程通过 OpenClaw + Claude Code 完成，用自然语言描述需求，AI 帮我把代码写出来，效率比自己手写高很多。

**访问地址：** https://ai-news-hub-next.vercel.app/`,
  },
  {
    title: '使用 OpenClaw 的三个高效经验',
    slug: `openclaw-efficient-tips-${Date.now()}`,
    content: `# 使用 OpenClaw 的三个高效经验

用了一段时间 OpenClaw，总结了三个让效率大幅提升的使用方式。

## 经验一：给 OpenClaw 一个任务列表

以前和 OpenClaw 对话，都是想到什么说什么，做完一件事再说下一件。后来发现可以直接给它一个任务列表，让它按优先级依次完成，效率高很多。

做法很简单，在 workspace 里维护一个 \`MY_TASKS.md\` 文件：

\`\`\`markdown
## 待办任务

| # | 任务 | 优先级 | 状态 |
|---|------|--------|------|
| 1 | 给博客加游客评论功能 | 高 | ⏳ 待执行 |
| 2 | AI 助手抽屉样式优化 | 中 | ⏳ 待执行 |
| 3 | 首页左侧加文章列表 | 低 | ⏳ 待执行 |
\`\`\`

告诉 OpenClaw"开始执行"，它就会按优先级逐个处理，完成后更新状态，全部做完通过 Telegram 通知我。

这样的好处是：我可以随时往列表里加任务，不用等上一个做完再说下一个。OpenClaw 有了全局视角，能更合理地安排执行顺序。

## 经验二：启动子 Agent 用 Claude Code 团队模式写代码

这是我觉得最强的用法。

以前让 OpenClaw 写代码，它会直接动手，期间我没法和它正常对话——它在写代码，我在等，体验很差。

后来发现可以让它启动一个子 Agent，在后台用 Claude Code 的团队模式完成代码任务，主对话完全不阻塞。

具体流程是：

1. 和 OpenClaw 讨论好方案，确认需求
2. 让它开一个子 Agent，通过 tmux 向 Claude Code 发送 \`/add-task\` 命令，把需求拆成多个子任务
3. 发送 \`/run-tasks\`，Claude Code 按顺序执行所有任务
4. 子 Agent 监控进度，测试通过后推送代码
5. 完成后通过 Telegram 通知我

整个过程我和 OpenClaw 继续正常聊天，代码在后台默默跑完，完全感觉不到等待。

就像叫了外卖，不用盯着门口等，外卖到了门铃会响。

## 经验三：用手机 Termius 远程连接服务器

OpenClaw 跑在云服务器上，有时候需要直接看终端状态。手机上装 Termius，配好 SSH 密钥，随时随地都能登上服务器。

连上后想看 Claude Code 正在干什么，直接：

\`\`\`bash
tmux attach -t claude-ui
\`\`\`

就能进入 Claude Code 的界面，看到它实时执行任务的过程。退出用 \`Ctrl+B D\`，不会关掉 session，Claude Code 继续在后台跑。

躺在床上用手机看服务器跑代码，这种感觉还挺奇妙的 😄

## 小结

这三个经验的核心思路是一样的：**把 OpenClaw 从被动响应变成主动执行**。给它任务列表，它知道该做什么；给它子 Agent，它知道怎么高效做；手机远程连接，随时随地都能掌控服务器状态。

用好这三个方式，OpenClaw 更像一个真正的助手，而不只是一个聊天机器人。

**访问地址：** https://ai-news-hub-next.vercel.app/`,
  },
]

async function main() {
  // 获取分类和标签
  const category = await prisma.category.findFirst()
  const tags = await prisma.tag.findMany({ take: 3 })
  const admin = await prisma.user.findFirst({ where: { role: 'admin' } })

  if (!admin) throw new Error('找不到 admin 用户')

  for (const article of articles) {
    const created = await prisma.article.create({
      data: {
        title: article.title,
        slug: article.slug,
        content: article.content,
        summary: article.content.slice(0, 200),
        published: true,
        authorId: admin.id,
        categoryId: category?.id,
        tags: {
          create: tags.slice(0, 2).map(tag => ({ tagId: tag.id }))
        }
      }
    })
    console.log(`✅ 创建文章: ${created.title} (id: ${created.id})`)
  }

  await prisma.$disconnect()
}
main().catch(console.error)
